# Only customizations live here. Run `atuin default-config` to see the
# full template with all available options.

# Enter selects and places the command on the prompt for editing
# (another Enter executes). Atuin's newer default is `true`, but the
# old install on this machine kept the legacy `false`; pinning so
# fresh installs match.
enter_accept = false
